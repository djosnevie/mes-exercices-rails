<h1>Les articles <a href="<%= new_post_path %>" class="btn btn-success">Créer un article</a> <hr></h1>
<% @posts.each do |post| %>

	<div>
		<h2><%= post.title %></h2>
		<p><%= post.content %></p>
		<p>
			<a href="<%= post_path(post.id) %>" class="btn btn-primary">Lire la suite...</a>  <a href="<%= edit_post_path(post.id) %>" class="btn btn-info">Editer l'article</a>  <a href="<%= post_path(post.id) %>" class="btn btn-danger" data-confirm="Etes-vous sur de Supprimer l'article" data-method="DELETE">Supprimer</a>
		</p>
		<hr>
	</div>

<% end %>



